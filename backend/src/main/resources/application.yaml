spring:
  security:
    user:
      name: admin
      password: admin123
  datasource:
    url: jdbc:postgresql://localhost:5432/learnrank
    username: learnrank
    password: localdevpassword
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8080
  
app:
  jwt:
    secret: ${JWT_SECRET:CHANGE_ME_MIN_32_CHARS_FOR_HS256_SECRET_KEY}
    access-token-ttl-ms: 3600000
    refresh-token-ttl-ms: 1209600000  
 
  